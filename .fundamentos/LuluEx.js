// Declare e atribua um valor para Cachorro usando let
let cachorro = true;

// Agora você pode usar a variável Cachorro em suas condições
if (cachorro = true) {
    console.log("cachorro");
}

// Declare e atribua um valor para Zebra usando let
let zebra = true;

// Agora você pode usar a variável Zebra em suas condições
if (zebra) {
    console.log("zebra");
}

// Se nenhuma das condições anteriores for atendida, retorne "humano"
console.log("humano");
